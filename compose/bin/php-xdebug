#!/usr/bin/env bash

if [ ! -d "src/bin" ]; then
  echo "Error: src/bin directory is missing. Please ensure the Magento source code is present in the src directory."
  exit 1
fi

bin/php -d xdebug.start_with_request=yes "$@"
